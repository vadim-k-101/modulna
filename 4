#include <stdio.h>
#include <math.h>

int main(void) {
  float a;
  const int x = 5;
  const int y = 7;
  const int z = 6;
  const int s = 9;

 a= (float)((sqrt(x)+5+5*5)/(sqrt(y)+7+7*7))+((sqrt(z)+6+6*6)/(sqrt(s)+9+9*9));

  
  printf("vidpovid= : %f \n",a);
  return 0;
}
